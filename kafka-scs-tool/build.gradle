apply plugin: 'java'
//apply plugin: 'eclipse-wtp'
apply plugin: 'org.springframework.boot'

jar {
    baseName = 'kafka-scs-tool'
    version = '0.0.1-SNAPSHOT'
}
sourceCompatibility = 1.8
targetCompatibility = 1.8

buildscript {
    ext {
		  springBootVersion = '1.5.9.RELEASE'
    }
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:${springBootVersion}")
    }
}

repositories {
    mavenCentral()
}


// spring-cloud-configを使う場合は以下のコメントをはずす
//dependencyManagement {
//    imports {
//        mavenBom 'org.springframework.cloud:spring-cloud-dependencies:Brixton.RELEASE'
//    }
//}

dependencies {
    compileOnly group: 'org.projectlombok', name: 'lombok', version: '1.16.10'
    compile 'org.springframework.boot:spring-boot-starter-web'
	compile "org.springframework.cloud:spring-cloud-starter-stream-kafka:${scsKafkaVersion}"
    compile "org.apache.kafka:kafka-clients:${kafkaClientVersion}"
	testCompile group: 'junit', name: 'junit', version: '4.12'
}
